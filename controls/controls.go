package controls

type Direction string

// Direction for the robot
const (
	LEFT  Direction = "LEFT"
	RIGHT Direction = "RIGHT"
	FRONT Direction = "FRONT"
	BACK  Direction = "BACK"
	STOP  Direction = "STOP"
)
